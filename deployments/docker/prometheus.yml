# Prometheus scrape config for GoGate local development.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: gogate
    metrics_path: /metrics
    static_configs:
      - targets:
          - gateway:8080
